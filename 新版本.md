# 简介：
新版本app,只用于安卓平台,使用原生kotiin开发,专注于专业算法和功能,界面简洁而优美

# 主要功能：
## 赛道管理：
### 功能：
- 热门赛道（官方发布,统一起始点
- 自定义赛道 （自己画）
### 逻辑实现：
- 热门赛道获取从统一地址,目前下方可以先定义track结构体,后实现web
- 自定义赛道,利用高德sdk,实现标注起点电子围栏,终点电子围栏,保存后属于自己的,可以上传云端,赛道创建人当前登录用户

## 开始记录：
### 功能：
- 赛道记录
  * 当前计时
  * 最近一次轨迹和本圈对比
  * g值记录
- 巡游模式
  * 当前计时
  * g值记录

### 逻辑实现(核心):
- 赛道记录
  * 可以点击选择赛道,默认选择为本赛道时间最近的一条轨迹,然后点击开始
  * gps通过手机默认的gps获取（1s一次），获取原生的gps点, 经过火星系坐标转换后,实时和我们赛道标注的起点开始比较,满足点击开始 & 经过起点 & 速度大于15km/h，启动定时器进行计时
  * 同步调用手机的加速度计,1秒获取25次！使用重力方向进行矫正，实时转换空间坐标,求出实际的加速度, 然后使用卡尔曼滤波进行过滤，拟合出当前的实际位置,在下一秒gps点到来的死后进行矫正积分
  * 当gps到达终点的时候！无须判断速度,结束本次,保存本次数据,然后展示本次的驾驶距离,时间,均速,最大速度,结束后保存的本次数据会有一个uid
  * 和历史轨迹比较功能实现逻辑就是,历史轨迹也是存在25hz的gps点,直接实时计算当前的distance,然后看看新旧花的时间,就直接显示+-时间差就行
  

## 我的轨迹
### 功能：
  - 一个轨迹列表
  - 轨迹数据展示1： 距离,均速,最大速度,时间,平均G值
  - 轨迹数据展示2： 距离 - 速度图
  - 轨迹数据对比：
        * 轨迹列表可以勾选2个,进行对比
        * 上面展示的都对比一波

### 实现逻辑：
* 轨迹数据来源1 本地保存的,可以执行按钮同步到云端
* 轨迹数据来源2 云端同步的,每个轨迹都会有uid,拉取云端和本地的,两个uid合并，云端优先
* 本地轨迹数据可以同步到云端
* 对比逻辑,主要就是展示一样的,唯一的难点计时 距离 - 速度图,就是用模拟器点算出距离+当前的时间画图,用不用颜色的线区分
  
## 视频合成功能：
### 功能：
 - 可以选择相册的视频
 - 可以选个一个轨迹
 - 进行视频合成,视频和轨迹可以手动拖动画轴进行时间同步
 - 合成后把速度，g值, 时间都放上去
  

## 用户中心
 - 登录（网络接口）
 - 修改资料,密码,头像（网络接口）
 - 展示累积里程
 - 用户标识


## 额外说明：
- 当前所有的需要的接口先做本地,先空着,测试的时候的响应地址就是本地模拟器一个万能ok的函数
- 语言使用kotlin原生开发
- 界面逻辑和实际的核心算法逻辑放到不到的代码文件夹,进行分级
- 界面简洁ui干爽
- 公用的函数.例如数据保存,上传,坐标系转换都特喵的抽出来做成公共函数
- 多封装代码和要清晰易懂可以复用,减少不必要的注释,ai开发下,智能体可以看懂
